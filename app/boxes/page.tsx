import Link from "next/link";
import Image from "next/image";
import { boxTypes } from "@/content/boxTypes";
import { SectionIntro } from "@/components/SectionIntro";

export default function BoxesPage() { return <div className="site-grid px-5 py-16 md:px-10 md:py-24"><div className="mx-auto max-w-[1400px]"><SectionIntro eyebrow="B2B packaging range" title="Formats for shipping, storage and handling." body="Start with the closest format. We will size it around your product, packing method, stacking requirement and delivery route." /><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{boxTypes.map((item) => <Link key={item.slug} href={`/boxes/${item.slug}`} className="card focus-ring group bg-paper p-5"><div className="relative aspect-[4/3] overflow-hidden border-2 border-ink bg-white"><Image src={item.image} alt={item.name} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw" className="object-contain transition-transform duration-300 group-hover:scale-105" /></div><h2 className="mt-5 text-3xl group-hover:text-ultra">{item.name}</h2><p className="mt-2 text-ink-soft">{item.description}</p><span className="mt-5 inline-block font-bold underline underline-offset-4">View format</span></Link>)}</div></div></div>; }
