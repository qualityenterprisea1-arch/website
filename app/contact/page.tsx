import Link from "next/link";
import { SectionIntro } from "@/components/SectionIntro";
import { site } from "@/content/site";

export default function ContactPage() { return <div className="site-grid min-h-[calc(100dvh-78px)] px-5 py-16 md:px-10 md:py-24"><div className="mx-auto max-w-[1200px]"><SectionIntro eyebrow="Contact the factory" title="Talk to the people who make the box." body="Send the basics and we will come back with a written answer. Or visit us in Narsingi." /><div className="mt-12 grid gap-5 md:grid-cols-[1fr_1.2fr]"><div className="card bg-paper p-6"><div className="grid gap-6">{[["Address", site.address], ["Phone", site.phone], ["Email", site.email], ["GSTIN", site.gstin], ["Hours", site.hours]].map(([label, value]) => <div key={label}><div className="mono text-xs uppercase tracking-[.14em] text-ink-soft">{label}</div><div className="mt-1 font-bold">{value}</div></div>)}</div><div className="mt-8 flex flex-wrap gap-3"><a href={`tel:${site.phone}`} className="pill focus-ring bg-kraft px-5 py-3 font-bold">Call us</a><a href={`https://wa.me/${site.whatsapp}`} className="pill focus-ring bg-ultra px-5 py-3 font-bold text-paper">WhatsApp</a><Link href="/quote" className="pill focus-ring bg-paper px-5 py-3 font-bold">Get a quote</Link></div></div><div className="card grid min-h-[420px] place-items-center bg-[#D7D4CB] p-6 text-center"><div><div className="mono text-xs uppercase tracking-[.14em]">Map embed slot</div><p className="mt-3 max-w-sm text-ink-soft">The Narsingi factory map will be embedded here once the verified Google Business Profile pin is supplied.</p></div></div></div></div></div>; }
