import { SectionIntro } from "@/components/SectionIntro";

export default function WorksPage() { return <div className="site-grid px-5 py-16 md:px-10 md:py-24"><div className="mx-auto max-w-[1200px]"><SectionIntro eyebrow="Capability gallery" title="A catalogue of formats, not made-up case studies." body="We do not have client projects to publish yet. This page shows the box formats and finishing options we can quote today." /><div className="mt-12 grid gap-5 md:grid-cols-3">{["RSC shipping blanks", "Printed mailer samples", "Heavy duty board", "Food and bakery formats", "Pads and partitions", "Corrugated rolls"].map((item) => <div key={item} className="card bg-paper p-5"><div className="grid aspect-[4/3] place-items-center border-2 border-ink bg-[#D7D4CB]"><span className="mono text-center text-xs uppercase tracking-[.12em]">Photo slot</span></div><h2 className="mt-5 text-3xl">{item}</h2><p className="mt-2 text-ink-soft">Sample image will be added when the matching factory photograph is ready.</p></div>)}</div><div className="card mt-8 bg-kraft p-7"><h2 className="text-4xl">Have a format in mind?</h2><p className="mt-4 max-w-2xl text-ink-soft">Send the dimensions and quantity. We will tell you what can run, what board it needs and what it will take to make.</p></div></div></div>; }
