export function SectionIntro({ eyebrow, title, body, light = false }: { eyebrow: string; title: string; body: string; light?: boolean }) { return <div className={light ? "text-paper" : ""}><div className="eyebrow">{eyebrow}</div><h2 className="mt-4 max-w-3xl text-5xl md:text-7xl">{title}</h2><p className={`mt-5 max-w-2xl text-lg ${light ? "text-paper/75" : "text-ink-soft"}`}>{body}</p></div>; }
